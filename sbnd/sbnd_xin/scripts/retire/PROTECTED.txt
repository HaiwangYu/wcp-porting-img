# sbnd_xin — arms that must survive the next retire round
#
# WHY THIS FILE EXISTS.  Every retire round writes its own dated driver
# (retire_20260730.sh, _20260802.sh, _20260803.sh), each with its own inline
# PROTECTED="..." string.  Nothing carries forward automatically: an arm named
# in one round's list is invisible to the next unless a human retypes it.  Doc
# pr/28 sec.15.9 flagged that for work-tfix388-r9 and it has been carried by
# memory ever since.  This file is the carry-forward list -- a new round's
# driver must union its inline PROTECTED with every line below.
#
# It does NOT retire anything and is not executable.  Removing a line is a
# decision to let that arm be swept; make it deliberately, and never remove one
# because a doc "already has the numbers" -- CLAUDE.md M13 protects the record
# itself, not just the summary of it.
#
# Format: <arm>  TAB  why it must survive  TAB  who cites it
# Field 1 may hold SEVERAL whitespace-separated arm names (the vf37 lines did).
# Housekeeping: when an arm is genuinely superseded, move its line to the
# RETIRED section at the bottom with the date, do not delete it.
#
# 2026-08-05: THIS FILE IS NOW ACTUALLY READ.  Until today no script parsed it
# -- retire_20260803.sh:30 hardcoded two names independently, so the "a new
# round's driver must union its inline PROTECTED with every line below"
# instruction above was aspirational.  retire_20260805.sh and
# plan_20260805.py both parse field 1 and word-split it.  Ten of the seventeen
# arms listed here were zero-citation, so a citation-driven tier rule would
# have swept them; that is the failure this file exists to prevent and it came
# within one round of happening.

work-tfix388-r9	round-9 track-fit gap-jumping arm; there is no automatic protection and it is not reproducible from any surviving input	doc pr/28 sec.15.9

# --- the post-flip cathode-rescue family (added 2026-08-17) ----------------
# Today's owner flip (doc 73 sec.12.9, toolkit 2d8c9e5a) turned SEVEN knobs
# ON in SBND production, superseding toolkit 771f075b (the pr87ion3 point
# below, now RETIRED).  work-cbr3-census-on is the new production reference:
# doc 73 sec.12.9 proves it with a BARE run_ql_batch.sh (no envs) reproducing
# its mabc-all-apa.zip + pctree member hashes 4/4 MATCH.  work-cbr3-bare2evt
# {,-pr} is the ONLY on-disk evidence the shipped jsonnet reproduces it --
# same role work-pr87-postflip-* played at 08-16, and the same lesson: that
# arm was dropped the SAME DAY it was created and its claim became text-only.
# The five work-*-vfcbr3on Q/L roots + five work-vf*-cbr3on PR out-roots are
# the post-flip product for the four samples census-on's tagger tail lacks
# (r1qlmc/r2mc/plus the -full nusel stage for all).  Each PR arm's Q/L input
# is re-verified against KEEP by plan_20260817.py ASSERT 8 on every run, not
# just hand-checked once.
# RELEASED 2026-08-19 (pass 2), see the RETIRED section: the four work-cbr3-*
# arms are superseded by prod0819 end-to-end.  Line kept commented so the
# parser stops protecting them; the provenance is at the bottom.
# RELEASED 2026-09-01b (doc 91), owner: "Peer is done, we can remove or
# retire them" + "go minimum".  THE WHOLE r1qlmc/r2mc SIM CHAIN goes together --
# both imaging hubs, both cb0805 Q/L hubs, both prod0813 PR arms, both -vfcbr3on
# Q/L roots and both vf*-cbr3on out-roots (10 dirs, 438 MB).  Released as a CHAIN
# so closure is preserved in both directions rather than by exempting a link.
# Grounds: neither sample is in the production four (nuecc48/ncpi0/mcp1k/mcp2k),
# nothing has written or read them since 2026-08-17, and the ":5017/:5018 LIVE"
# protection this file recorded had ALREADY lapsed -- the note two blocks above
# says so in its own words.
# COST, stated: the r1qlmc and r2mc samples leave the tree entirely.  There is no
# PR product, no Q/L root and no imaging for either after this round; re-entering
# them means imaging from reco1.  doc pr/76's campaign numbers become text-only.
# work-r1qlmc-vfcbr3on work-r2mc-vfcbr3on	post-flip Q/L for the two SIM samples; NOT reprocessed by prod0819, so cbr3on is still their latest	doc 73 sec.12.5, docs/work-tags.md 2026-08-19
# part of the same chain -- see the block on the line above.
# work-vfr1qlmc-cbr3on work-vfr2mc-cbr3on	latest PR out-root for each SIM sample; reads the -vfcbr3on Q/L root above	doc 73 sec.12.5, docs/work-tags.md 2026-08-19

# --- the r1qlmc/r2mc prod0813 family (added 2026-08-13, still active) -----
# 2026-08-19: THE VIEWER PIN IS RELEASED.  Owner, asked directly during the
# doc pr/95 round: "you can turn off display in 5017 5018".  There was in
# fact nothing to turn off -- `pgrep -af 'bokeh|serve_pr_display'` returns
# nothing and `ss -ltnp` shows nothing listening on 5013-5019, so the
# ":5017/:5018 LIVE" ground recorded on 2026-08-13 had already lapsed some
# time before this round and nobody had noticed.  Do not re-derive a
# protection from it.
#
# These two survive on the OTHER stated ground only, which still holds:
# they are the only FULL-coverage PR product for either sim sample (10 and
# 13 pr_evt vs the cbr3on arms' nu_evaluated subset of 4 and 6), neither
# sim sample is in the doc pr/95 campaign, and the pair costs 30 MB.  If a
# later round wants them gone, that is now a one-line decision with no
# viewer to check first.
# No pr83/85/86/87 arm exists for either MC sample -- prod0813 remains their
# latest and only PR product.  Also LIVE on the bokeh viewers, ports 5017/5018.
# part of the same chain -- see the r1qlmc/r2mc block above.
# work-r1qlmc-prod0813 work-r2mc-prod0813	the two sim samples of the doc pr/76 campaign; only surviving PR products for either MC sample; LIVE on bokeh :5017/:5018	doc pr/76

# --- the prod0819 single-epoch baseline (added 2026-08-19) ----------------
# doc pr/95.  The FIRST product in this tree where Q/L and PR come from ONE
# software epoch (toolkit fd6a116d, libWireCellClus.so md5 3fae4385f9dc27fa63ac280faebb71df)
# -- with ONE documented exception: a concurrent session relinked the library at
# 23:04:06 (toolkit f0e69780, a log-only env-gated probe), so 1889 of
# work-mcp2k-prod0819's 2000 events ran on md5 75652e607a7f3d7ceec2c0632dd2e9f5.
# The two binaries were PROVEN byte-equivalent on this chain -- 30 archives, 15
# nusel TSVs, 3 calib-pr dumps identical (work-pr96gate-mcp2k, -nuedisp; doc
# pr/95 sec.4b).  Each arm's .build-fingerprint carries its own record.
# Every prior campaign mixed epochs -- prod0813 ran the PR chain onto 08-05
# cb0805 pctrees, and census-on's Q/L predates the pr/94 flip.  This is the
# reference tomorrow's PR rounds A/B against, so it is the one family that
# must not be swept by a routine round.
#
# READ THIS BEFORE COMPARING ANYTHING TO IT: since fd6a116d production emits
# ONE T_tagger/T_kine ROW PER IN-BEAM FLASH BUNDLE.  Row 0 is NOT the event's
# answer -- scripts/pr94_rows.py primary_index() is.  pr_scores_table.py
# already applies it; anything else that hard-indexes [0] is wrong.
# RELEASED 2026-08-25b (doc 81 sec 10), see the RETIRED section.  Superseded by
# work-<s>-grp0825, which doc 81 sec 7 proved carries the SAME Q/L products
# member-for-member (24536/24536 with the imaging half).  The reference side is
# frozen in scripts/retire/state-20260825b/hashes/stagea-<s>.tsv before deletion,
# so that PASS stays re-checkable.
# work-nuecc48-ql0819 work-ncpi0-ql0819 work-mcp1k-ql0819 work-mcp2k-ql0819	prod0819 Q/L roots (48/19/1000/2000 evts), bare production at toolkit fd6a116d; input to the PR baseline below	doc pr/95
# RELEASED 2026-08-23 (see RETIRED section): owner chose to DROP the prod0819 PR
# arms and re-run production for all four samples at current HEAD instead.  The
# four -ql0819 Q/L roots above are UNCHANGED and remain the campaign input.
# work-nuecc48-prod0819 work-ncpi0-prod0819 work-mcp1k-prod0819 work-mcp2k-prod0819	THE PR baseline: full 13-stage chain over 3067 evts, PR_EXTRA_STAGES=pr_display; each reads its matching -ql0819 root	doc pr/95

# --- doc pr/102 missing-orphan-segment audit (added 2026-08-20) ------------
# RELEASED 2026-08-23, owner: doc pr/102 r2 has shipped (len_admit=30 ON)
# work-pr102-head-mcp1k work-pr102-dbg-mcp1k	pr/102 "after" epoch (fresh mcp1k at HEAD, pr_display + traj_cover_probe) and its reserved debug arm; also the round READS work-{mcp1k,mcp2k}-prod0819 above as before-epoch + relocated pr/96 calibration events	doc pr/102, docs/work-tags.md 2026-08-20
# work-pr102r2-offfull-mcp1k work-pr102r2-onfull-mcp1k work-pr102r2-on-nuecc48 work-pr102r2-on-ncpi0 work-pr102r2-onA-mcp1k work-pr102r2-onA-mcp2k	pr/102 round-2 P1+P2 validation arms (census before/after + physics ledgers + exhibit smoke); owner scan pending	doc pr/102 round 2, docs/work-tags.md 2026-08-20

# --- the current-production PR arms (added 2026-08-23) --------------------
# doc pr/104 shipped SBND production ON 2026-08-21 (vertex_junction_snap +
# vjs_override_kink_snap; toolkit a07222e2 + cfg flip c550541f).  Docs pr/105
# and pr/106 then measured the selection stack and left production UNCHANGED,
# and pr/107-111 are studies -- so pr/104 is still the production epoch on
# disk.  -on4 is the env-forced product (48/19/1000 full, mcp2k 15-evt
# subset); -flipchk is the post-flip BARE run (no env) that proves the
# shipped jsonnet reproduces it, PASS 30/30 + 38/38 + 96/96 (doc pr/104 sec.6).
# Keeping -flipchk is the lesson PROTECTED.txt records twice below
# (work-pr87-postflip-*, work-cbr3-bare2evt): drop the bare-config arm and the
# "shipped config == validated arm" claim becomes text-only the same day.
#
# THESE ARE SUPERSEDED THE MOMENT the 2026-08-23 re-run campaign lands with
# full coverage for all four samples.  Release them in that follow-up pass,
# not before -- until it exists they are the tree's ONLY PR product for the
# three data samples.
# --- prod0823: THE production PR baseline (added 2026-08-23, same day) -----
# The re-run the owner authorised the prod0819 release against.  Full coverage
# for all four data samples -- 48 / 19 / 1000 / 2000 = 3067 events, every one
# rc=0 -- bare production (no SBND_* overrides), PR_EXTRA_STAGES=pr_display,
# toolkit b5c9f43a, libWireCellClus.so md5 628444a7de4f9d224288b0ebf7c34e20,
# each arm reading its matching -ql0819 Q/L root.  This is what mcp2k has not
# had since the pr/104 flip: a 2000-event PR product at current production.
#
# PROVEN a same-epoch continuation of the pr/104 arms, not a new epoch:
# pr85_hash_gate vs work-pr104-on4-* is PASS 96/96 (nueCC48), 38/38 (NCpi0),
# 2000/2000 (mcp1k), 30/30 (the mcp2k 15-evt overlap), and vs the bare-config
# work-pr104-flipchk-* another 96/96 + 38/38 -- 2164 archives byte-identical.
# nusel-{table,events}.tsv identical on both small samples.  CONTROL, so the
# PASS is not a stale-binary artefact (M1): tracking-pr.root DIFFERS on 10/10
# nueCC48 events checked -- the three unknobbed pr/109 commits (a46b0ddb,
# dd4d1373, 56683366) changed the T_proj_data ROOT dump and nothing else.
# Consequence: any A/B a doc took against work-pr104-on4-* stays valid against
# these arms.
# RELEASED 2026-08-25b (doc 81 sec 10), owner: "I assume we can safely retire
# ... and recover the disk".  prod0823 is explicitly PRE-flip (doc 81 sec 4) and
# prod0825 is the production epoch on disk.  Its one remaining job -- serving as
# the revert-proof reference -- was already exercised and the result is text in
# doc 81 sec 4.
#
# COST, stated rather than absorbed: this was the LAST on-disk carrier of the
# pr/104 production epoch, so every A/B docs pr/104 through pr/111 took against
# that epoch is now TEXT-ONLY.  Knowingly the same shape as the pr94r3 and
# work-pr87-postflip-* losses recorded below, taken deliberately.
# What survives it: scripts/retire/state-20260825b/hashes/work-<s>-prod0823.tsv,
# 9201 member-content rollups (3 products x 3067 events).  Read that as
# INSURANCE, not gate preservation -- prod0825 sits at a different operating
# point, so there was never a byte-identity claim between the two to keep
# re-checkable.  What it buys is a future revert-reproduction: re-run at toolkit
# b5c9f43a and diff against the frozen file.
# work-nuecc48-prod0823 work-ncpi0-prod0823 work-mcp1k-prod0823 work-mcp2k-prod0823	THE production PR baseline, 3067 evts at toolkit b5c9f43a; reads the -ql0819 roots (ASSERT 8)	docs/work-tags.md 2026-08-23

# doc 81 (2026-08-25): the group-mode re-baseline.  work-<s>-grp0825 is stage A
# (imaging + Q/L, per-event layout) and work-<s>-prod0825 is stage B (PR) at the
# CURRENT operating point -- dl_vtx_dual_chain/snapD2 (doc pr/112) and
# fast_xgb_forest (doc 76 r1), both flipped ON AFTER prod0823 was produced.
# Stage A is byte-identical to work-img-<s> + work-<s>-ql0819 on all 3067 events
# (24536/24536 archives, scripts/multi/stagea_gate.py); stage B is byte-identical
# to the pr112i-snapD2/flipchk arms.  These are the MCS and PR baseline.
#
# 2026-08-25b: BOTH reference sides named above are now retired, so grp0825 is
# the sole carrier of stage A -- imaging AND Q/L -- for all four data samples.
# Its protection is correspondingly harder: there is no longer a second copy of
# these products anywhere in the tree, only the frozen member-content rollups in
# state-20260825b/hashes/stagea-<s>.tsv.  A future round that releases a grp0825
# arm is deleting the product itself, not one copy of two.
# 2026-09-02 (doc 98): grp0825 IS NOW IMAGING-ONLY.  The round split the arm:
# its evt<N> imaging is the substrate 10478 symlinks across the tree resolve
# into and is untouchable; its ql_evt<N> half was TWO operating points stale
# (doc 97 sec 2 measured today's knob-off run failing to reproduce it -- epoch
# drift) and nothing anywhere symlinked to it, so it was retired in place after
# rollup_grp0825_ql_20260902.py froze its 18402-member hash rollup into
# state-20260902/grp0825-ql-rollup.tsv.  Do NOT read the line below as still
# protecting Q/L products; the current Q/L is work-*-d97fv.
work-nuecc48-grp0825 work-ncpi0-grp0825 work-mcp1k-grp0825 work-mcp2k-grp0825	doc 81 stage A IMAGING (evt<N>) only -- 48/19/1000/2000 evts; ql_evt<N> retired 2026-09-02	docs/81_group-mode-production.md, doc 98

# --- the pr/104 arms: NOW SUPERSEDED, kept pending an owner decision -------
# prod0823 above covers every sample at least as widely and is byte-identical
# on every overlapping event, so these 8 arms (4.3 G) are redundant and are
# the OBVIOUS release candidate for the next round.  NOT released here: that
# is an owner call, and the round that created prod0823 should not also be the
# round that destroys the thing it was gated against.
# RELEASED 2026-08-25 (doc 81), see the RETIRED section: work-pr104-on4-nuecc48 ...
# RELEASED 2026-08-25 (doc 81), see the RETIRED section: work-pr104-flipchk-nuecc48 ...

# --- the current vertex-label epoch (added 2026-08-23) --------------------
# 1756 of vertex_labels/vtxscan-vtx105-*'s `source` entries resolve into these
# four arms -- the NEWEST label epoch (docs pr/105, pr/106).  The label JSON is
# self-contained and survives regardless; these arms are what keeps the calib-pr
# dumps behind them re-openable (pr85_panels2d.py, pr86_kink_census.py,
# vtx_rules/carry_labels.py).  Every older epoch's dumps (harv3, prod0813,
# vtx100) is already gone -- see the RETIRED section; this is the last one left.
work-vtx105-base-nuecc48 work-vtx105-base-ncpi0 work-vtx105-base-mcp1k work-vtx105-base-mcp2k	calib-pr dumps behind the current vtxscan-vtx105-* label epoch (47/19/407/581 evts)	docs/pr/105, docs/pr/106, owner 2026-08-23

# --- doc pr/111's live inputs (added 2026-08-23) --------------------------
# The OPEN round.  Five scripts hardcode these two paths -- scripts/pr111_
# {brittle,ensemble,dl_decomp,rival,rival_where}.py -- as the fit_exclusion
# ON/OFF pair for the DL-vertex brittleness measurement.  Not a citation
# inference: grep of scripts/ finds the absolute paths.
# RELEASED 2026-08-25 (doc 81), see the RETIRED section: work-vtx106-harv-base-nuecc48 ...
# RELEASED 2026-08-25 (doc 81), see the RETIRED section: work-vtx106-cne-on-nuecc48 ...

# --- non-reproducible crash evidence (added 2026-08-23) -------------------
# 2026-08-25b: MADE SELF-CONTAINED, and this is why the round's ASSERT 4 earns
# its keep.  This arm's evt178410/ was a SYMLINK into work-img-mcp2k, and its
# ql_evt178410/*.npz linked through it -- so retiring the imaging hubs would
# have left five broken links inside the one arm this file calls
# not-re-capturable, silently, with no error at delete time.  The link was
# replaced by the real bytes it pointed at (cp -rL) BEFORE any deletion; the arm
# grew 6.7 MB -> 17 MB and now depends on nothing outside itself.
work-probe178410a	the ONLY on-disk proof mcp2k evt 178410's SIGSEGV is non-deterministic (rc=0 / 683 MB at -j 1 vs rc=139 / 2403 MB at -j 32); a nondeterministic crash cannot be re-captured on demand; 17 MB, self-contained	doc pr/97

# --- the production baseline at the PINNED operating point (added 2026-09-01) --
# doc 89 Phase 1.  work-*-prod0901 was the production set until this round, but
# it finished 05:00 and the save_in_scope flip (toolkit d52d818c, pinned in
# ref/prod-2026-09-01b) landed 13:36 -- so its tracking-pr.root has no
# T_cluster tree and it was one flip behind the operating point it was supposed
# to represent.  These four arms ARE that operating point: full output (no
# suppression knob set, PR_EXTRA_STAGES=pr_display), binary and cfg tree both
# pinned, prod_cfg_gate.py PASS 21/21.
#
# They replaced prod0901 only after interlock 7's successor gate showed all
# 3067 events reproducing on every SHARED product with T_cluster the only
# added tree.  Do not release these without a successor that passes the same
# gate; "a newer arm exists" is not the same claim.
# RELEASED 2026-09-02 (doc 98).  This pin was created by doc 91's owner
# instruction "We want to keep the latest production though" -- and prod0901b
# is no longer the latest; work-*-d97fvpr2 is.  The same sentence that
# protected it releases it.  Its 31/31 sentinel role is carried by
# work-sent97-*, which was built and verified BEFORE this was deleted.
# work-nuecc48-prod0901b work-ncpi0-prod0901b work-mcp1k-prod0901b work-mcp2k-prod0901b	the SBND production baseline at ref/prod-2026-09-01b	doc 89

# --- doc 87 acceptance evidence: REFUSED release 2026-09-01 ----------------
# The doc-89 planner wanted to release the four three-sample gate arms
# (work-87scope-on-*, work-87tc-off-*, work-87mrg-post-*, work-87knob-def-*,
# 4.2 G) as "superseded".  Refused, for the same reason the 08-31 round refused
# prod0825: doc 87 shipped the SAME DAY and these arms are the acceptance
# evidence for a knob that moved the production operating point.  doc 89's
# successor gate is a DIFFERENT comparison (prod0901 vs prod0901b) and does not
# re-establish doc 87 sec 6.1's "knobs at their defaults change nothing" table.
#
# work-87knob-min-* and work-87knob-sup-* are additionally the ONLY
# minimal-output arms in the tree; doc 87 sec 6.2's table rests on them and
# nothing else can rebuild it.
#
# RELEASE CONDITION, written down so it is a decision and not an inference:
# these go when doc 87 has a settled successor round that re-establishes sec
# 6.1 and sec 6.2 at production scale -- not merely when doc 87 is "closed".
# RELEASED 2026-09-01b (doc 91).  THE RELEASE CONDITION THIS FILE WROTE ON
# 2026-09-01 IS MET FOR THESE TWELVE, and only for these twelve.  It read: "these
# go when doc 87 has a settled successor round that re-establishes sec 6.1 and
# sec 6.2 at production scale -- not merely when doc 87 is closed."
# sec 6.1 and sec 1.4 ARE re-established, at 3067 events instead of 482, and not
# by inference: doc 89's successor gate compared work-*-prod0901 (toolkit
# ddce7430, which PREDATES the doc-87 knobs entirely) against work-*-prod0901b
# (knobs present, at their defaults, post-merge) and found every shared product
# identical on all 3067 events.  "Knobs at their defaults change nothing" and
# "the merge changes nothing" are exactly what that measured.  The evidence
# survives the arms: scripts/retire/state-20260901/successor-gate.tsv, one row
# per event, which is what makes the claim re-checkable after the arms are gone.
# sec 6.2 is NOT re-established -- prod0901b ran FULL output -- so
# work-87knob-{min,sup}-* stay, and so do the group-mode and flip arms.  The
# condition was applied section by section, not to the block.
# work-87scope-on-mcp1k work-87scope-on-ncpi0 work-87scope-on-nuecc48	doc 87 sec 4 save_in_scope acceptance (308/308)	doc 87
# same twelve-arm release -- see the block above.
# work-87tc-off-mcp1k work-87tc-off-ncpi0 work-87tc-off-nuecc48	doc 87 sec 1.4 pre-merge baseline	doc 87
# same twelve-arm release -- see the block above.
# work-87mrg-post-mcp1k work-87mrg-post-ncpi0 work-87mrg-post-nuecc48	doc 87 sec 1.4 post-merge gate arm	doc 87
# same twelve-arm release -- see the block above.
# work-87knob-def-mcp1k work-87knob-def-ncpi0 work-87knob-def-nuecc48	doc 87 sec 6.1 knobs-at-defaults gate arm	doc 87
work-87knob-min-ncpi0 work-87knob-min-nuecc48 work-87knob-sup-ncpi0 work-87knob-sup-nuecc48	the ONLY minimal-output arms in the tree; doc 87 sec 6.2	doc 87
work-87flip-ncpi0 work-87flip-nuecc48	doc 87 sec 4.6 flip-equivalence	doc 87
work-87grp-def-ncpi0 work-87grp-min-ncpi0 work-87grp-nopct-ncpi0	doc 87 sec 6.4 group-mode arms	doc 87
# RELEASED 2026-09-01b (doc 91).  The SBND_PR_CALIB matrix: 7 arms, 1 ncpi0
# event each, 21 MB -- the worst dir-count-per-byte in the tree, which is what
# this round was called to fix.  Re-runnable end to end from work-ncpi0-grp0825
# (kept) by re-running the sec 6.5 matrix; it is a knob sweep, not a measurement
# that cannot be repeated.  doc 87 sec 6.5's table stays as the record.
# work-87calA-ncpi0 work-87calB-ncpi0 work-87calC-ncpi0 work-87cal2A-ncpi0 work-87cal2B-ncpi0 work-87cal2C-ncpi0 work-87cal2D-ncpi0	doc 87 sec 6.5 SBND_PR_CALIB matrix, 21 MB total	doc 87

# --- the bare work/ dir (added 2026-09-01) --------------------------------
# NOT an arm.  It matches the planner's d.startswith('work') discovery and
# holds work/nusel_labels/ -- a hand-scan record (M13) -- plus
# work/pr137_sheets/.  ASSERT 2 would have caught it via the label-archive
# check, but a record directory should be named, not rescued by a safety net.
work	bare work/ -- nusel_labels/ hand-scan record (M13) + pr137_sheets/	doc 89

# --- doc 98: the 2026-09-02 production retention set --------------------------
# The owner's instruction was "we only need to save the latest production
# results for Q/L matching as well as PR results".  After doc 97's flip that is
# these eight dirs, and they are the reason every other full-sample arm went.
work-mcp1k-d97fv work-mcp2k-d97fv work-ncpi0-d97fv work-nuecc48-d97fv	LATEST PRODUCTION stage A (Q/L) at ref/prod-2026-09-04, sep_fv_point ON, 3067 evts	doc 97, doc 98
# --- STAGE B MOVED 2026-09-06 (doc 101) ----------------------------------
# doc pr/144 sec 16.3.1 cut a new full-sample stage B -- work-*-d144fixprod,
# 3067/3067 rc=0, the COMMITTED DEFAULT on the crash-fixed binary -- and
# re-baselined the sentinel suite onto it.  So the line below is no longer
# "LATEST PRODUCTION stage B"; the line under it is.  The 09-06 round put the
# d97fvpr2 arms in TIER 2, which is the tier that needs an explicit owner yes
# precisely BECAUSE it edits this file rather than only the disk.
# What survives the arm: products/prod0902/ (the committed reference tables,
# computed from d97fvpr2), ref/prod-2026-09-04's README, and the frozen
# member-content manifests in archive/records/cleanup-20260906/sbnd-tier2/.
# RELEASED 2026-09-06 (doc 101 tier 2) -- move to the RETIRED section when the
# owner confirms; until then this line still protects the arms.
work-mcp1k-d97fvpr2 work-mcp2k-d97fvpr2 work-ncpi0-d97fvpr2 work-nuecc48-d97fvpr2	the PREVIOUS production stage B (3067 evts, ref/prod-2026-09-04); superseded by work-*-d144fixprod -- doc pr/144 sec 16.3.1	doc 97, doc 98, doc 101
work-mcp1k-d144fixprod work-mcp2k-d144fixprod work-ncpi0-d144fixprod work-nuecc48-d144fixprod	**LATEST PRODUCTION stage B (PR tail)**, 3067/3067 rc=0, the committed default on the crash-fixed binary; the sentinel suite is re-baselined onto it.  Reads work-*-d97fv (pr144_arms.sh:52).  Pin /home/xqian/tmp/d144_libpin4	doc pr/144 sec 16.3.1, doc 101

# --- the post-d144fixprod flips (added 2026-09-06, doc 101) ---------------
# FOUR flips landed AFTER d144fixprod was cut, so "latest production" is a SET,
# not one arm, and there is no full-sample arm at today's operating point:
#   pr/145 item 4  kine_near_pointing_impact 200 / miss_deg 30   SBND ON 09-06
#   pr/146         ang_sv, default OFF; kine_sat_cont_keep_deg 25 RECOMMENDED
#   pr/147         long_muon_cathode_bridge_track_types          SBND ON 09-06
#   pr/147         ..._tail_min_len 20 cm                        SBND ON 09-06
# Each arm below is the on-disk evidence for one of them.  Dropping the
# bare-config / flip-equivalence arm is the mistake this file already records
# twice (work-pr87-postflip-*, work-cbr3-bare2evt): the "shipped config ==
# validated arm" claim becomes text-only the same day.
work-mcp1k-d145prod work-mcp2k-d145prod work-ncpi0-d145prod work-nuecc48-d145prod	pr/145 item 4 SHIPPED arm: no PR_EXTRA_TLA at all, 35/35 rc=0, gated against work-*-d144fixprod (4 events move, all four the owner called cosmics)	doc pr/145 sec 8.3
work-d147-tailflip-mcp1k work-d147-tailflip-mcp2k work-d147-flipchk-mcp1k work-d147-flipchk-mcp2k	pr/147's two flip-equivalence arms -- 34/34 for the tail-absorb flip, 44/44 for track_types.  These are the post-flip BARE runs that prove the shipped jsonnet reproduces the validated arm	doc pr/147 sec 15.1
work-mcp1k-d146sv25 work-mcp2k-d146sv25 work-ncpi0-d146sv25 work-nuecc48-d146sv25	pr/146 sec 12's RECOMMENDED but not-yet-flipped operating point (kine_sat_cont_keep_deg 25).  An open owner decision needs its arm on disk	doc pr/146 sec 12
work-mcp1k-d145np work-mcp2k-d145np work-ncpi0-d145np work-nuecc48-d145np	the full-sample (3067) arm doc pr/148 sec 16 names as what the NEXT round reads -- item 3's bisect and 137238's log lines.  A doc whose last section is a plan for the next session is an OPEN round	doc pr/148 sec 16
#
# THE SENTINEL WITNESS.  ITS ORIGINAL REASON IS SPENT, and the honest restatement
# is below.  It was built because production was 30 PASS / 1 FAIL and 105074
# (pr/128 class B) was an OPEN owner call needing an arm to adjudicate against.
# The owner retired that sentinel on 2026-09-02 ("feel free to retire") and the
# production suite is now 30 PASS / 0 FAIL, so the witness is no longer
# load-bearing for it.  KEPT ANYWAY, for a different and smaller reason: at 165
# MB it is the last surviving PR arm from BEFORE the sep_fv_point flip, which is
# what lets a future round tell "a shipped fix died" from "the epoch moved" --
# the doc pr/127 failure mode.  Release it freely if that stops being worth 165 MB.  The only 31/31 arms on disk were
# work-*-d97off2pr and work-*-prod0901b and doc 98 released BOTH, so the 28
# distinct sentinel events were copied out first.  Deleting this strands the
# open sentinel with nothing to adjudicate against.  Rebuild, if it is ever
# lost, is NOT possible from production -- production is the arm that fails.
work-sent97-mcp1k work-sent97-mcp2k work-sent97-ncpi0 work-sent97-nuecc48	the 31/31 sentinel witness carved out of d97off2pr before it was retired; 165 MB	doc 98, scripts/retire/witness_sentinels_20260902.py
#
# work-d97prodchk-* SCORES ZERO IN A NAME-EXACT CITATION CENSUS and is kept
# anyway.  It is doc 97 sec 9's proof that the PRODUCTION DEFAULT (no env flag)
# reproduces the validated d97fv arm byte-for-byte -- the doc references it at
# line 588 as a path inside a table cell, which a token-level census does not
# score.  This is doc 91's own failure mode (a census returning 0 for a genuinely
# cited arm is indistinguishable from one that works).  Do not release it on a
# zero-citation reading.
work-d97prodchk-mcp1k work-d97prodchk-mcp2k work-d97prodchk-ncpi0 work-d97prodchk-nuecc48 work-dbg25a-d97prodchk	doc 97 sec 9: production default == the validated arm, 139 evts. AMENDED 2026-09-04 (doc 100 sec 4): the four sample arms are whole; work-dbg25a-d97prodchk is NOT -- its 20 ql_evt<N> OUTPUTS (mabc, pctree, logs), which are what sec 9 measures, are intact, but its imaging INPUT is gone (work-dbg25a-ql was deleted under it) so it can be re-read, NOT re-run. Regenerable via dbg25_stage.sh -> dbg25_run.sh -> d97_dbg25_arm.sh	doc 97, doc 98, doc 100
#
# A LIVE PEER ROUND OWNS work-*-doc25new* / work-*-doc25old (PDVD doc 25's SBND
# regression-gate arms, gate round 7 in flight 2026-09-02).  Protected by
# PREFIX for the life of that round -- the same treatment doc 91 gave doc 90's
# arms.  Release when the PDVD doc 25 round closes, not before.

# --- doc 100, the 2026-09-04 round (added 2026-09-04) ---------------------
# Owner: "keep the latest production result as well as their input", and, asked
# which depth, "honour PROTECTED.txt".  So the display/sentinel block below and
# every doc-87 line survived this round untouched.  77 dirs / 12.6 GiB released;
# records in archive/records/campaign-close-20260904 (integrity 77/77).
#
# INTERLOCK 5 REFUSED TWO ARMS THAT WERE ON THE FIRST DRAFT of RETIRE_LABELS --
# work-probe178410a and work-dbg25a-d97prodchk.  Both are lines in this file.
# That is the second round running in which this file has vetoed a deletion the
# planner would otherwise have made; it is not decoration.
work-ncpi0-d99r3prod work-ncpi0-d99r3prodpr	the ref/prod-2026-09-05 flip's own arm (doc 99 r3): knobs ON in the jsonnet defaults with no TLA, i.e. the path production actually takes.  The NEWEST operating point on disk -- work-*-d97fvpr2 is 3067 events but at prod-2026-09-04	doc 99 r3, doc 100
#
# *** DAMAGE RECORD, doc 100 sec 4 -- work-dbg25a-d97prodchk IS NO LONGER
# SELF-CONTAINED. ***  This round deleted work-dbg25a-ql, which it borrowed its
# imaging input from, because INTERLOCK 3 was VACUOUS for absolutely-spelled
# symlinks (it compared realpath() against ROOT written with the /nfs/data/1
# alias, and /nfs/data/1/xqian/toolkit-dev is itself a symlink to
# /home/xqian/toolkit-dev, so the comparison could never match an arm name).
# Interlock 3 is fixed in plan_20260904.py -- realpath BOTH sides, full depth --
# and proven against the exact link it missed.
# What survives in that arm: all 20 ql_evt<N>/ with their mabc-*.zip,
# pctree-evt<N>.tar.gz and logs -- i.e. everything doc 97 sec 9 measures.
# What is gone: the 20 evt<N> input links and the 105 MB of icluster-apa*.npz
# behind them.  REGENERABLE: input_files_reco1/stm_tagger_feedback/
# debug-25evt-reco1.root (708 MB), the 26 staged-dbg25/ dirs and
# bee/dbg25/dbg25.manifest.tsv all survive; dbg25_stage.sh -> dbg25_run.sh ->
# d97_dbg25_arm.sh rebuilds it.  NOT rebuilt -- re-creating a just-deleted label
# is an owner call (M13).
#
# --- doc 100 round C, 2026-09-04 -----------------------------------------
# RELEASED: work-em114-* and work-em114c-* (6 arms, 1.2 GiB).  Their ground was
# "live em_display manifests" and it was FALSE -- measured, the manifests
# reference 420 arms of which only TEN still exist, and none of them is an
# em114/em114c arm.  The earlier "cited" reading came from substring-matching
# em114 inside work-em114c-prodnowdbg-mcp1k, a different and already-deleted arm.
#
# NOT RELEASED, though the owner authorised the block, because measuring it said
# otherwise -- each of these has a named live consumer:
#   work-pr130r1-probe98-* work-pr130r1-probe141-* work-pr134-f086-*
#       the ONLY ten arms any em_display manifest still resolves into
#   work-vtx105-base-*
#       878 hand-scan label files under vertex_labels/vtxscan-vtx105-* reference
#       these 1724 times.  My claim that the dl_vtx_training ground was stale was
#       WRONG -- I grepped scripts/ and dl_vtx_training/ is top-level.  M13.
#   work-*-d97prodchk work-dbg25a-d97prodchk
#       pdvd docs 31 and 37 use its ql_evt<N> as the SBND cross-detector
#       reference, and doc 37 is the CURRENT pdvd epoch.
#
# --- RETIRED (kept for provenance) ---------------------------------------
#
# RELEASED 2026-08-31b (doc pr/135 sec 11.2), owner-approved after the direct
# question "Do we need 0825prod?".  Traced through the CONSUMERS, not the
# assert: every metric script (pr132_pi0_census, pr132_gamma_ledger,
# pr135_pi0_peak_prod, pr126_pi0_census, pr126_pi0_mass, pr132_angle_census)
# reads m_cur and NONE loads the scan-time manifest.  What the scan-time
# reconstruction recorded per gamma survives in the 298 label JSONs.
# GIVEN UP: pr126-pi0-mass.tsv is no longer rebuildable.
# work-nuecc48-prod0825 work-ncpi0-prod0825 work-mcp1k-prod0825 work-mcp2k-prod0825  |  doc 81 stage B PR at the current operating point; ASSERT 8 reads its group build log  |  docs/81_group-mode-production.md
# RELEASED 2026-08-31b (doc pr/135 sec 11.2), owner-approved after the direct
# question "Do we need 0825prod?".  Traced through the CONSUMERS, not the
# assert: every metric script (pr132_pi0_census, pr132_gamma_ledger,
# pr135_pi0_peak_prod, pr126_pi0_census, pr126_pi0_mass, pr132_angle_census)
# reads m_cur and NONE loads the scan-time manifest.  What the scan-time
# reconstruction recorded per gamma survives in the 298 label JSONs.
# GIVEN UP: pr126-pi0-mass.tsv is no longer rebuildable.
# work-pr124r1-onA98-mcp1k work-pr124r1-onA98-mcp2k work-pr124r1-onA98-ncpi0 work-pr124r1-onA98-nuecc48  |  dump source for 50 hand-paired pi0 (em_display/pr126-pi0-manifest.tsv) and the 98-set EM census; the EM-scale fit cannot be re-checked without them  |  docs/pr/126 sec 3-4, owner 2026-08-29
# RELEASED 2026-08-31b (doc pr/135 sec 11.2), owner-approved after the direct
# question "Do we need 0825prod?".  Traced through the CONSUMERS, not the
# assert: every metric script (pr132_pi0_census, pr132_gamma_ledger,
# pr135_pi0_peak_prod, pr126_pi0_census, pr126_pi0_mass, pr132_angle_census)
# reads m_cur and NONE loads the scan-time manifest.  What the scan-time
# reconstruction recorded per gamma survives in the 298 label JSONs.
# GIVEN UP: pr126-pi0-mass.tsv is no longer rebuildable.
# work-pr124r1-onA141v2-mcp1k work-pr124r1-onA141v2-mcp2k  |  the 141-set half of the same pi0 manifest  |  docs/pr/126 sec 3
# RELEASED 2026-08-31b (doc pr/135 sec 11.2), owner-approved after the direct
# question "Do we need 0825prod?".  Traced through the CONSUMERS, not the
# assert: every metric script (pr132_pi0_census, pr132_gamma_ledger,
# pr135_pi0_peak_prod, pr126_pi0_census, pr126_pi0_mass, pr132_angle_census)
# reads m_cur and NONE loads the scan-time manifest.  What the scan-time
# reconstruction recorded per gamma survives in the 298 label JSONs.
# GIVEN UP: pr126-pi0-mass.tsv is no longer rebuildable.
# work-pr125r1-flipchk98-mcp1k work-pr125r1-flipchk98-mcp2k work-pr125r1-flipchk98-ncpi0 work-pr125r1-flipchk98-nuecc48  |  doc pr/126 sec 4f re-runs the identification census at the pr/125 production point out of these  |  docs/pr/126 sec 4f
# RELEASED 2026-08-31b (doc pr/135 sec 11.2), owner-approved after the direct
# question "Do we need 0825prod?".  Traced through the CONSUMERS, not the
# assert: every metric script (pr132_pi0_census, pr132_gamma_ledger,
# pr135_pi0_peak_prod, pr126_pi0_census, pr126_pi0_mass, pr132_angle_census)
# reads m_cur and NONE loads the scan-time manifest.  What the scan-time
# reconstruction recorded per gamma survives in the 298 label JSONs.
# GIVEN UP: pr126-pi0-mass.tsv is no longer rebuildable.
# work-pr125r1-flipchk141-mcp1k work-pr125r1-flipchk141-mcp2k  |  the 141-set half of the sec 4f census  |  docs/pr/126 sec 4f

#
# RELEASED 2026-08-31 (doc pr/135 retire round): doc pr/130 is reported and
# CLOSED, which is the release condition block 5 stated for itself -- "these
# go when doc pr/130 is reported and closed, in the same pass that releases
# the pr128r1-on gate label above".  Both halves done in this one pass.  The
# six work-pr130r1-probe* arms are the stated EXCEPTION and stay active
# above: their line claims non-reproducibility, not mere summarisation.
# work-pr128r1-on98-mcp1k work-pr128r1-on98-mcp2k work-pr128r1-on98-ncpi0 work-pr128r1-on98-nuecc48 work-pr128r1-on141-mcp1k work-pr128r1-on141-mcp2k  |  the gate reference doc pr/130 item 1 cites by label; doc pr/128 SHIPPED but the citing round is OPEN  |  docs/pr/130, peer session 2026-08-29
# RELEASED 2026-08-31 (doc pr/135 retire round): doc pr/130 is reported and
# CLOSED, which is the release condition block 5 stated for itself -- "these
# go when doc pr/130 is reported and closed, in the same pass that releases
# the pr128r1-on gate label above".  Both halves done in this one pass.  The
# six work-pr130r1-probe* arms are the stated EXCEPTION and stay active
# above: their line claims non-reproducibility, not mere summarisation.
# work-pr129gf-nokine-mcp2k work-pr129probe-171572 work-pr129probe-mcp1k work-pr129probe-mcp2k work-pr129pt-meas-mcp2k work-pr129pt-on-mcp2k work-pr129r1-flipchk-mcp2k work-pr129r1-off-mcp1k work-pr129r1-off-mcp2k work-pr129r1-off-ncpi0 work-pr129r1-off-nuecc48 work-pr129r1-on141-mcp1k work-pr129r1-on141-mcp2k work-pr129r1-on98-mcp1k work-pr129r1-on98-mcp2k work-pr129r1-on98-ncpi0 work-pr129r1-on98-nuecc48  |  doc pr/129 SHIPPED today; its on98/on141 arms are the open round's production baseline  |  docs/pr/129, peer session 2026-08-29
# RELEASED 2026-08-31 (doc pr/135 retire round): doc pr/130 is reported and
# CLOSED, which is the release condition block 5 stated for itself -- "these
# go when doc pr/130 is reported and closed, in the same pass that releases
# the pr128r1-on gate label above".  Both halves done in this one pass.  The
# six work-pr130r1-probe* arms are the stated EXCEPTION and stay active
# above: their line claims non-reproducibility, not mere summarisation.
# work-pr130-292643-guardoff work-pr130-47212-guardoff work-pr130-gs1-mcp1k work-pr130-gs1-mcp2k work-pr130-gs1-nuecc48 work-pr130-neutral-mcp2k work-pr130-probe-mcp2k work-pr130-probe2-mcp2k work-pr130-qx1-mcp1k work-pr130-qx1-mcp2k work-pr130-vtx-mcp1k work-pr130-vtx-mcp2k  |  doc pr/130 OPEN -- probe, guard-off and census arms of the live round  |  docs/pr/130, peer session 2026-08-29
# RELEASED 2026-08-31 (doc pr/135 retire round): doc pr/130 is reported and
# CLOSED, which is the release condition block 5 stated for itself -- "these
# go when doc pr/130 is reported and closed, in the same pass that releases
# the pr128r1-on gate label above".  Both halves done in this one pass.  The
# six work-pr130r1-probe* arms are the stated EXCEPTION and stay active
# above: their line claims non-reproducibility, not mere summarisation.
# work-pr130r1-g1off-mcp1k work-pr130r1-g1off-mcp2k work-pr130r1-g1off-ncpi0 work-pr130r1-g1off-nuecc48 work-pr130r1-g1off141-mcp1k work-pr130r1-g1off141-mcp2k work-pr130r1-gd141off-mcp1k work-pr130r1-gd141off-mcp2k work-pr130r1-gs1on-mcp1k work-pr130r1-gs1on-mcp2k work-pr130r1-gs1on-ncpi0  |  doc pr/130's LIVE 239-event knob-off gate, being written while this round ran  |  docs/pr/130, peer session 2026-08-29


# RELEASED 2026-08-25 by doc 81, after work-<s>-prod0825 passed its gates.
# COST, stated rather than absorbed: doc pr/104's flip-equivalence evidence and
# doc pr/111's fit_exclusion pair become TEXT-ONLY, and doc 81's own stage-B
# PASS against work-pr112i-snapD2-* / -flipchk-* becomes text-only the same day.
# The owner accepted that explicitly; prod0825 is the reference from here on.
# work-pr104-on4-nuecc48 work-pr104-on4-ncpi0 work-pr104-on4-mcp1k work-pr104-on4-mcp2k	current-production PR product (junction snap ON), 48/19/1000/15 evts; each reads its matching -ql0819 root (ASSERT 8)	doc pr/104 sec.5
# work-pr104-flipchk-nuecc48 work-pr104-flipchk-ncpi0 work-pr104-flipchk-mcp1k work-pr104-flipchk-mcp2k	the ONLY on-disk evidence the shipped post-flip jsonnet reproduces -on4 with no env	doc pr/104 sec.6
# work-vtx106-harv-base-nuecc48 work-vtx106-harv-nofitx-nuecc48	fit_exclusion ON/OFF pair read by scripts/pr111_*.py (open round)	docs/pr/111
# work-vtx106-cne-on-nuecc48 work-vtx106-cne-off-nuecc48	the pr/106 sec.10 dl_vtx_cloud_no_exclusion pair; listed in doc pr/111's own arm table (sec.2), which is why a script grep alone would have missed it	docs/pr/111 sec.2
# 2026-08-23 -- the prod0819 PR arms and the whole pr/102 family, RELEASED.
#
# Owner, asked directly 2026-08-23 during the 203 G -> minimal round: "we can
# drop this, and redo the production for the samples, so we keep the latest PR
# production for all samples".  So the 08-19 baseline's PR layer goes and a
# fresh full-coverage campaign at current HEAD replaces it.  The four -ql0819
# Q/L roots are NOT released -- they are that campaign's input.
#
# COST, stated before running and accepted: doc pr/95's single-epoch baseline
# is the reference docs pr/98-104 A/B'd against, and after this round those
# comparisons are text-only.  mcp2k loses its only 2000-event PR product until
# the re-run lands (work-pr104-on4-mcp2k covers 15 of 2000).  Also released
# with it: work-pr96gate-{mcp2k,nuedisp} and work-pr96-{dbg1,dbg2,dbg3,fx1}
# -mcp2k / work-pr96gate-disp -- the pr96gate pair was kept in 08-19 pass 1
# solely to prove the mid-campaign relink was byte-equivalent ON THE PROD0819
# ARMS (doc pr/95 sec.4b); with those arms gone the proof has no subject.
#
# work-nuecc48-prod0819 work-ncpi0-prod0819 work-mcp1k-prod0819 work-mcp2k-prod0819	the 08-19 single-epoch PR baseline; owner released 2026-08-23 in favour of a fresh full-coverage re-run at HEAD	doc pr/95, docs/work-tags.md 2026-08-23
# work-pr96gate-mcp2k work-pr96gate-nuedisp	mixed-binary equivalence proof for the prod0819 arms; released with its subject	doc pr/95 sec.4b
# work-pr102-head-mcp1k work-pr102-dbg-mcp1k work-pr102r2-offfull-mcp1k work-pr102r2-onfull-mcp1k work-pr102r2-on-nuecc48 work-pr102r2-on-ncpi0 work-pr102r2-onA-mcp1k work-pr102r2-onA-mcp2k	doc pr/102 audit + round-2 validation arms; the "owner scan pending" note dated 08-20 is discharged -- r2 shipped len_admit=30 ON and pr/103+pr/104 have flipped production twice since	docs/pr/102, docs/work-tags.md 2026-08-23
#
# 2026-08-19 (pass 2) -- the whole cbr3 family, the three DATA-sample valfast
# cbr3on arms, and the 21 doc-pr/94 r3 flip-evidence arms.  SUPERSEDED by the
# prod0819 baseline above, which is single-epoch where all of these were not.
#
# Owner decision, asked directly 2026-08-19: retire ALL 21 work-pr94r3* arms.
# They were kept through pass 1 as the only on-disk proof of that day's
# production flip; pass 2 releases them, and dropping the two mcp2k arms is
# what unpinned work-cbr3-census-on (9.6 GiB, the largest pass-1 survivor).
#
# COST, stated before running and accepted: doc pr/94's flip-equivalence gates
# (bare == env-forced ON: 96/96+48/48 nuecc48, 16/16+8/8 mcp1k, 28/28+14/14
# mcp2k) and its pre-flip escape-hatch reproduction (96/96) become TEXT-ONLY.
# This is knowingly the same shape as the work-pr87-postflip-* loss recorded
# below -- taken deliberately this time, not by oversight, because prod0819
# IS the production reference from here on.  Also text-only: doc 73 sec.12.9's
# bare-run 4/4 member-hash proof, whose arms were census-on + bare2evt.
#
# work-cbr3-census-on work-cbr3-census-pr-on work-cbr3-bare2evt work-cbr3-bare2evt-pr	SUPERSEDED by prod0819 (single-epoch); census-on was also the Q/L input for the pr94r3 mcp2k arms, released the same round	doc 73 sec.12.9, doc pr/95
# work-mcp1kall-vfcbr3on work-ncpi0-vfcbr3on work-nuecc48-vfcbr3on	SUPERSEDED by work-{mcp1k,ncpi0,nuecc48}-ql0819	doc pr/95
# work-vfmcp1k-cbr3on work-vfncpi0-cbr3on work-vfnuecc48-cbr3on	SUPERSEDED by work-{mcp1k,ncpi0,nuecc48}-prod0819	doc pr/95
# work-pr94r3-off-nuecc48 work-pr94r3-off-ncpi0 work-pr94r3-on-nuecc48 work-pr94r3-on-mcp1k work-pr94r3-on-mcp2k work-pr94r3-flip-nuecc48 work-pr94r3-flip-mcp1k work-pr94r3-flip-mcp2k work-pr94r3-flipA-nuecc48 work-pr94r3b-off-nuecc48 work-pr94r3-preflip-nuecc48	doc pr/94 Phase 6 flip gates + escape-hatch reproduction; owner released 2026-08-19	docs/pr/94, docs/work-tags.md 2026-08-19
# work-pr94r3-a1only-116962 work-pr94r3-a1only-395148 work-pr94r3-a1only-73038 work-pr94r3-a2only-116962 work-pr94r3-a2only-395148 work-pr94r3-a2only-73038	doc pr/94 per-knob attribution arms (pr94r3_gap_metric.py)	docs/pr/94
# work-pr94r3-ql22 work-pr94r3-ql22disp work-pr94r3-ql65053r2 work-pr94r3-ql65053r3	doc pr/94 Q/L-side arms; ql22disp was the Bee -q source for bee/pr94r3 (already built and uploaded, the zip survives under bee/)	docs/pr/94
#
# 2026-08-17 -- work-{mcp1k,nuecc48,ncpi0}-pr87ion3, SUPERSEDED by today's
# seven-knob flip; work-mcp2k-cb0816 + the four work-*-harv3 arms, RELEASED
# by owner narrowing "latest" to post-flip only.
#
# pr87ion3 (toolkit 771f075b, doc pr/86 sec.15) was yesterday's production
# reference; today's flip (toolkit 2d8c9e5a, doc 73 sec.12.9) supersedes it.
# See the active entry above for its replacement, work-cbr3-census-on.
#
# work-mcp2k-cb0816 (mcp2k's Q/L hub) and work-{mcp1k,mcp2k,nuecc48,ncpi0}
# -harv3 (the dl_vtx_harvest-generation PR arms) are pre-flip and are not
# "latest or input" under the owner's post-flip-only reading.  COST,
# owner-confirmed before running: vtx_rules/baselines.py's
# deployed_dump_path() (which rewrites -prod0813 -> -harv3) stops resolving
# AGAIN, and vtx_rules/scankit.py:858's three named harv3 arms are gone. The
# 1543 vertex_labels/ hand-scan labels (13 tags, self-contained JSON) survive
# -- same precedent as uitest75/vtxscan1 and the 08-16 -prod0813 drop -- but
# the calib dumps those scans were made against do not.  mcp2k has no
# complete PR product after this round, only the 40 events in
# work-cbr3-census-pr-on.
#
# work-mcp1k-pr87ion3 work-nuecc48-pr87ion3 work-ncpi0-pr87ion3	SUPERSEDED by work-cbr3-census-on (toolkit 2d8c9e5a); the cb0805 Q/L hubs it read remain KEEP as valfast infrastructure	doc 73 sec.12.9, docs/work-tags.md 2026-08-17
# work-mcp2k-cb0816	SUPERSEDED by work-cbr3-census-on (mcp2k's Q/L is now inside census-on, not a standalone hub); RELEASED, owner narrowing to post-flip only	docs/work-tags.md 2026-08-17
# work-mcp1k-harv3 work-mcp2k-harv3 work-nuecc48-harv3 work-ncpi0-harv3	pre-flip PR arms; baselines.deployed_dump_path() no longer resolves; mcp2k left with no complete PR product	docs/pr/88, docs/pr/89, docs/work-tags.md 2026-08-17
# 2026-08-16 -- work-{mcp1k,nuecc48,ncpi0}-prod0813 and the three plain
# -ma10 arms, SUPERSEDED / narrowed by owner request.
#
# The 2026-08-13 prod0813 family above named all five -prod0813 arms.  Same
# day as the pr87ion3 round, the owner asked to narrow work* further to "only
# the latest one, as well as input to achieve it" per sample.  pr87ion3 is
# the latest for mcp1k/nuecc48/ncpi0 and its input is the cb0805/img hubs
# (already KEEP), so -prod0813 and -ma10 for those three samples are no
# longer "latest or input" and retired here (followup2_20260816.py, archived
# to archive/records/pr79-86-era-20260816/{prod0813-arms,ma10-family}/).
# r1qlmc/r2mc-prod0813 are UNCHANGED -- see the active entry above.
#
# COST, owner-confirmed before running: 481 of 483 vertex_labels/ entries
# have `source` inside one of these -prod0813 dumps; the label JSON itself
# (main_vertex, picks[], scores) is self-contained and survives, but the
# underlying calib dump those scans were made against does not -- no more
# re-rendering pr85_panels2d.py / pr86_kink_census.py against those events.
# baselines.py's deployed_dump_path() (which rewrote -prod0813 -> -ma10) no
# longer resolves, so the 473-event DL-vertex analysis set (docs pr/77-81) is
# not re-derivable; the already-built dl_vtx_training/data/* snapshots still
# back every number those docs quote.
#
# work-mcp1k-prod0813 work-nuecc48-prod0813 work-ncpi0-prod0813	SUPERSEDED by work-*-pr87ion3; vertex_labels/ source is now unopenable for these events, labels themselves survive	doc pr/86 sec.15, docs/work-tags.md 2026-08-16
# work-mcp1k-ma10 work-nuecc48-ma10 work-ncpi0-ma10	SUPERSEDED by work-*-pr87ion3; baselines.deployed_dump_path() no longer resolves	docs/pr/77-81, docs/work-tags.md 2026-08-16
# work-pr87-postflip-mcp1k work-pr87-postflip-nuecc48 work-pr87-postflip-ncpi0	the 21-event proof pr87ion3 == bare production; dropped in the SAME follow-up round as "not latest or input" -- the claim survives only in doc pr/86 sec.15.4 text now, not on disk	doc pr/86 sec.15.4, docs/work-tags.md 2026-08-16
# 2026-08-13 -- the 2026-08-11 reference family, SUPERSEDED.
#
# work-pr64r4-{on48,on19,on1k} (current-production PR reference / Bee sources)
# and work-pr64r4-{scan48,scan19} (oc56 truth-table dumps) were retired by the
# 2026-08-13 round, archived to archive/records/pr66-75-era-20260813/.  They
# were never actually protected by this file -- the 2026-08-11 entry was a
# COMMENT block, so the parser (which takes field 1 of each non-comment line)
# never saw a single one of those five names.  That is why the prod0813 family
# above is written as real tab-delimited lines instead.
#
# KNOWN COST, recorded rather than discovered later: scripts/analysis/pr57/
# oc56_truth.py's DEFAULT_ARMS loses all three of its arms.  Two retire here;
# the third name it lists, work-pr64-scan1k, was ALREADY stale (the disk had
# work-pr64r4-scan1k).  The oc56 truth table is not recomputable until someone
# runs a fresh PR_OC56_SCAN_DUMP=1 arm.  Owner-confirmed 2026-08-13.
#
# P3 (the determinism floor owed since 2026-08-05, see the vf37 entry at the
# bottom) is PARTIALLY discharged by this round: work-gate0813-merged48 vs
# work-gate0813-repeat48 is a matched-layout A/A' pair on the current binary
# with no rebuild between -- 48/48 identical on pctree, mabc, rc AND the
# log-derived nusel TSVs (run_pr_chain_batch.sh:1067 already runs each event
# under `setarch x86_64 -R`, so both arms are ASLR-disabled).  What is STILL
# owed is the cross-layout leg: one ASLR-ON arm compared against those two,
# the pr37_a2_floor.sh shape.  Do not read "48/48" as the full floor.
# 2026-08-11 -- the pr/33 clean-source family + pr/37b repeat pair +
# nuecc48-prod0803/vfnuecc48-prod0803, SUPERSEDED.
#
# All were the pre-campaign reference: the pr/33 pair as "the only baseline
# until the re-processing campaign lands" (see their entry below, dated
# 2026-08-05) and prod0803/pr37b as the pr/28-37 era's input root and
# determinism floor.  The re-processing campaign (doc 71, 2026-08-05) and the
# pr/62+pr/64+pr/65 production flips since have superseded both roles --
# current production is gated against work-pr64r4-{on48,on19,on1k} instead
# (2026-08-11 reference family above).  work-pr37b-repeatA/B and
# work-nuecc48-prod0803/work-vfnuecc48-prod0803 were already removed in the
# 2026-08-05 clean-slate round; work-pr33-base48/off48 existed on disk again
# at the 2026-08-11 round (regenerated after the cs round) and were retired
# with this round's sweep, archived to
# archive/records/pr38-65-era-20260811/pr30-39-era/.
#
# work-pr37b-repeatA	repeat-run determinism FLOOR at toolkit 2457320d, arm A (setarch -R, PR_EXTRA_STAGES=pr_display); the pair is the only floor measurement that covers the calib dump	doc pr/37 sec.6.2
# work-pr37b-repeatB	same pair, arm B; a floor is a PAIR -- deleting either one destroys the measurement, not half of it	doc pr/37 sec.6.2
# work-nuecc48-prod0803	the 48-event nueCC input root every pr/28-37 arm is generated from	doc pr/33 sec.11, doc pr/37 Repro
# work-vfnuecc48-prod0803	its valfast counterpart	scripts/retire/retire_20260803.sh
# work-pr33-base48	the clean-source reference; pr/33's knob-off gate is base48 vs off48 48/48 cross-binary	doc pr/33 sec.11.2, doc pr/37 sec.13
# work-pr33-off48 work-pr33-prod48 work-pr33-allon48	pr/33's knob-off / production / all-on arms -- the gate is the SET, not any one arm	doc pr/33 sec.11.1
# work-pr33-f1aon48 work-pr33-f1bon48 work-pr33-f2aon48 work-pr33-f2bon48 work-pr33-f2con48 work-pr33-f3on48 work-pr33-f4on48 work-pr33-f5on48	per-knob attribution arms; f4on48 is the only mover (17/48) and the rest are the evidence that the other seven are null	doc pr/33 sec.11.1
# work-pr33-headchk work-pr33-freshchk	controls of the three-build reproducibility proof that established the stale-binary finding	doc pr/33 sec.11.2
# 2026-08-05 -- the twelve vf37 arms, RELEASED EARLY BY OWNER OVERRIDE.
#
# Read this before concluding the release condition was met: IT WAS NOT.  The
# entry below used to say these arms stay "until the population campaign has
# run against the promoted gate ... Owner's call to drop them after."  The
# campaign has NOT run.  The owner released them anyway, explicitly, on
# 2026-08-05, because doc pr/33 sec.11.2 established that they were produced by
# the stale-object binary -- so no future run can be gated against them and the
# condition could never be satisfied as written.  Their compare summary lives in
# doc pr/37 sec.2.5 (0/629 exact, both address layouts) and doc pr/37 sec.13.5
# records that the arms themselves are gone.
#
# THE SUCCESSOR IS OWED.  These were the only A/A' floor measurement on the
# widened seven-tree gate.  The re-processing campaign must include a repeat
# pair on the clean binary (two arms, no rebuild between, setarch -R, plus one
# ASLR-on leg -- the pr37_a2_floor.sh shape) or the tree has no determinism
# floor at all.  Tracked as precondition P3 in docs/work-tags.md.
#
# work-vfmcp1k-vf37a work-vfnuecc48-vf37a work-vfr1qlmc-vf37a work-vfr2mc-vf37a	A/A' floor arm A, setarch -R	doc pr/37 sec.2.5
# work-vfmcp1k-vf37b work-vfnuecc48-vf37b work-vfr1qlmc-vf37b work-vfr2mc-vf37b	A/A' floor arm B, setarch -R (matched layout)	doc pr/37 sec.2.5
# work-vfmcp1k-vf37c work-vfnuecc48-vf37c work-vfr1qlmc-vf37c work-vfr2mc-vf37c	cross-layout arm C, ASLR ON	doc pr/37 sec.2.5

# =========================================================================
# --- the 2026-08-29 round: the pi0 epoch, the hand-scan display defaults,
#     the sentinel controls, and the open pr/130 gate  --------------------
# =========================================================================
#
# WHY THIS BLOCK EXISTS.  The 08-29 round sweeps eleven CLOSED doc families
# (pr/117-pr/128, doc 84 r1-r4, doc 114's em114*) -- 442 arms, 83 G.  Almost
# every arm in them is an A/B leg whose verdict is already text in its doc.
# The names below are the exceptions, and each is an exception for a DIFFERENT
# reason.  A glob over the retiring family prefixes would sweep all of them,
# so they are spelled out: plan_20260829.py ASSERT 7 refuses the round if any
# line here is missing from KEEP.
#
# 1. THE pi0 EPOCH.  The owner's stated next major move is pi0 reconstruction
# driven by the hand-scan results.  doc pr/126 fit the EM charge scale
# (kine_shower_fudge_factor 0.80 -> 0.84, PEAK estimator) off 50 hand-paired
# pi0, and em_display/pr126-pi0-manifest.tsv keys those 50 to calib-pr dumps
# inside work-pr124r1-onA{98,141v2}-*.  doc pr/126 sec 4f re-ran the same
# census at the pr/125 production point out of work-pr125r1-flipchk{98,141}-*.
# The hand-scan LABELS are safe either way -- em_labels/ is 2 MB and outside
# work-* -- but the dumps they are keyed to are not, and a label with no dump
# cannot be re-fit.  Deleting these would make the 0.84 recommendation
# text-only BEFORE the round that acts on it.  Verified this round: all three
# manifests resolve 50/50, 98/98 and 141/141 (ASSERT 11).
#
# 2. HAND-SCAN DISPLAY DEFAULTS.  These are hardcoded DEFAULT arm names inside
# the scan tooling, not citations: em_display/prep_pr117.py:17,28 and
# em_display/prep_pr121.py:15.  Sweep them and the next scoring round breaks
# only when someone runs the scorer without explicit arm args -- a silent trap,
# which is exactly the failure ASSERT 10 was invented for (vtx_rules/
# baselines.py, scripts/analysis/pr57/oc56_truth.py, both found weeks late).
# The peer session confirmed these by grepping its own tools, not from memory.
# work-pr117r1-onK1-{mcp2k,ncpi0,nuecc48} are NOT script defaults; they are
# added so em_display/em117-117onK1-manifest.tsv resolves 98/98 rather than
# 25/98, which keeps the 98-set display baseline whole for 558 MB.
# RELEASED 2026-09-01b (doc 91).  THE STATED GROUND WAS WRONG, and this round
# checked it instead of inheriting it.  "hardcoded default arms in
# em_display/prep_pr121.py:15" -- that line is a docstring USAGE example, not an
# argparse default; `grep -n "default=" em_display/prep_pr121.py` matches no arm
# name at all.  The same mis-read protected work-pr117r1-onK1-mcp1k on the line
# below.  Both fall under the record-script rule, so they are ACKed in
# plan_20260901b.py rather than kept.
# The 141-set and 98-set displays are superseded anyway: em114c-134f086141- and
# em117-134f08698-manifest.tsv, backed by the kept work-pr134-f086-* arms, are
# the same event sets at the 0.86 EM scale that doc pr/135 made production.
# work-em114c-prodnow-mcp1k work-em114c-prodnow-mcp2k	hardcoded default arms in em_display/prep_pr121.py:15 (141-set display)	peer session 2026-08-29, verified by grep
# same mis-read -- see the block above.
# work-pr117r1-onK1-mcp1k	hardcoded default arm in em_display/prep_pr117.py:17,28 (98-set display)	peer session 2026-08-29, verified by grep
# RELEASED 2026-09-01b (doc 91).  em117-117onK1-manifest.tsv is retired from
# LIVE_MANIFESTS with them and prints in ASSERT 11's STATED COST list.
# work-pr117r1-onK1-mcp2k work-pr117r1-onK1-ncpi0 work-pr117r1-onK1-nuecc48	the other three samples of the same 98-set display baseline; keeps em117-117onK1-manifest.tsv whole (98/98) for 558 MB	docs/pr/117, 08-29 round
#
# 3. THE SENTINEL REGISTRY'S NEGATIVE CONTROL.  scripts/pr127_sentinels.py:31
# names work-pr125r1-flipK5* as the canonical arm on which a sentinel is
# EXPECTED to fail (the 137238 FAIL).  A registry of passing sentinels with no
# worked failing case cannot be trusted to fail when it should -- doc pr/127
# exists because a shipped fix died silently for ten days.
# NOTE, found while checking this: the same docstring at line 30 also names
# work-pr127r1-flipS{98,141}-* as its primary usage example, and those arms do
# NOT exist on disk and were not created by this round's sweep -- the reference
# was already stale before today.  Recorded here, not silently fixed; the peer
# session owns that file.
# RELEASED 2026-09-01c (doc 91 sec 13), owner: "please go ahead" on the
# re-baseline.  BOTH grounds these lines rested on are gone, and were checked
# rather than assumed.
#
# work-sent130neg* were the negative-control layer at the 2026-08-29 operating
# point -- before the 0.86 EM scale flip, doc 77 r3/r4's 17 retired TLAs and the
# master merge.  doc 91 sec 12 re-baselined every threshold they bracketed, so
# they now bracket numbers nobody uses.  Their replacement, work-91neg-*, was
# measured at the pinned prod0901b point against the NEW thresholds and is
# protected by name in plan_20260901c.py's KEEP_PREFIX.
#
# work-pr125r1-flipK5* were kept as "the worked FAILING sentinel case (137238)".
# sec 12 re-baselined 137238 and the assertion DIRECTION flipped, so what these
# arms demonstrate is no longer what the suite asserts.  Its control is now
# work-91neg-scccgap-nuecc48.
#
# The release is guarded, not asserted: ASSERT 16's falsifiability check names
# one control per re-baselined sentinel and RE-DERIVES its FAIL by running the
# suite, so this cannot silently leave a threshold green and unfalsifiable.
# Proven able to fire -- drop any one control and it refuses that sentinel.
# work-pr125r1-flipK598-mcp1k work-pr125r1-flipK598-mcp2k work-pr125r1-flipK598-ncpi0 work-pr125r1-flipK598-nuecc48	the sentinel registry's negative control (expect the 137238 FAIL); scripts/pr127_sentinels.py:31	docs/pr/127, peer session 2026-08-29
# RELEASED 2026-09-01c (doc 91 sec 13), owner: "please go ahead" on the
# re-baseline.  BOTH grounds these lines rested on are gone, and were checked
# rather than assumed.
#
# work-sent130neg* were the negative-control layer at the 2026-08-29 operating
# point -- before the 0.86 EM scale flip, doc 77 r3/r4's 17 retired TLAs and the
# master merge.  doc 91 sec 12 re-baselined every threshold they bracketed, so
# they now bracket numbers nobody uses.  Their replacement, work-91neg-*, was
# measured at the pinned prod0901b point against the NEW thresholds and is
# protected by name in plan_20260901c.py's KEEP_PREFIX.
#
# work-pr125r1-flipK5* were kept as "the worked FAILING sentinel case (137238)".
# sec 12 re-baselined 137238 and the assertion DIRECTION flipped, so what these
# arms demonstrate is no longer what the suite asserts.  Its control is now
# work-91neg-scccgap-nuecc48.
#
# The release is guarded, not asserted: ASSERT 16's falsifiability check names
# one control per re-baselined sentinel and RE-DERIVES its FAIL by running the
# suite, so this cannot silently leave a threshold green and unfalsifiable.
# Proven able to fire -- drop any one control and it refuses that sentinel.
# work-pr125r1-flipK5141-mcp1k work-pr125r1-flipK5141-mcp2k	141-set half of the same negative control	docs/pr/127
#
# 4. THE GATE LABEL THE OPEN ROUND CITES.  doc pr/130's item-1 work reports its
# probe arms as "gated SAME against work-pr128r1-on*".  CLAUDE.md's "report
# gates by label" convention exists so a PASS can be re-checked later; deleting
# the reference side while the citing round is still OPEN would defeat it.
# Releasable once doc pr/130 closes -- but not by inference, by a decision.
#
# 5. THE OPEN pr/130 ROUND (peer session).  A concurrently-running session owns
# a live 239-event knob-off gate for two new default-OFF knobs
# (shower_pass4_prox_guard_len, shower_pass3_backfill_guard_len).  It asked
# directly: "please leave the whole work-pr130* prefix alone until I say the
# gate is reported."  work-pr129* is its current-production baseline and
# work-sent130* is the sentinel suite guarding today's three flips.
# RELEASE CONDITION, stated so a future round does not have to guess: these go
# when doc pr/130 is reported and closed, in the same pass that releases the
# pr128r1-on gate label above.  This round protected the whole prefix rather
# than an enumerated subset precisely because the peer was still CREATING arms
# while the plan ran -- work-pr130r1-gs1on-* and work-pr130r1-g1off141-*
# appeared between the first census and the plan.
work-pr130r1-probe98-mcp1k work-pr130r1-probe98-mcp2k work-pr130r1-probe98-ncpi0 work-pr130r1-probe98-nuecc48 work-pr130r1-probe141-mcp1k work-pr130r1-probe141-mcp2k	dump source for BOTH pr130q manifests and the census source for pr130_launder_scan.py; the 239-event blast-radius result is not reproducible without them	docs/pr/130, peer session 2026-08-29
# RELEASED 2026-09-01b (doc 91), the POSITIVE half only.  MEASURED, not
# assumed: work-*-prod0901b was checked to contain all 30 pr127_sentinels.py
# events itself (the suite locates an event by scanning arm roots for pr_evt<N>/,
# and 30/30 resolve), so the suite runs against production and no longer needs
# its own arms.  work-sent130-{mcp1k,mcp2k} existed because production used to be
# an event SUBSET; it is not any more.  Re-derived by ASSERT 15 on every run.
# The NEGATIVE controls stay, on the line below, and are now held by a prefix.
# work-sent130-mcp1k work-sent130-mcp2k work-sent130neg-mcp1k work-sent130neg-mcp2k work-sent130neg2-mcp1k work-sent130neg2-mcp2k work-sent130neg3-mcp2k work-sent130neg4-mcp2k	the sentinel suite guarding today's pr/127-129 flips, incl. the negative controls scripts/pr127_sentinels.py:153-165 names	docs/pr/127, docs/pr/130
# RELEASED 2026-09-01c (doc 91 sec 13), owner: "please go ahead" on the
# re-baseline.  BOTH grounds these lines rested on are gone, and were checked
# rather than assumed.
#
# work-sent130neg* were the negative-control layer at the 2026-08-29 operating
# point -- before the 0.86 EM scale flip, doc 77 r3/r4's 17 retired TLAs and the
# master merge.  doc 91 sec 12 re-baselined every threshold they bracketed, so
# they now bracket numbers nobody uses.  Their replacement, work-91neg-*, was
# measured at the pinned prod0901b point against the NEW thresholds and is
# protected by name in plan_20260901c.py's KEEP_PREFIX.
#
# work-pr125r1-flipK5* were kept as "the worked FAILING sentinel case (137238)".
# sec 12 re-baselined 137238 and the assertion DIRECTION flipped, so what these
# arms demonstrate is no longer what the suite asserts.  Its control is now
# work-91neg-scccgap-nuecc48.
#
# The release is guarded, not asserted: ASSERT 16's falsifiability check names
# one control per re-baselined sentinel and RE-DERIVES its FAIL by running the
# suite, so this cannot silently leave a threshold green and unfalsifiable.
# Proven able to fire -- drop any one control and it refuses that sentinel.
# work-sent130neg-mcp1k work-sent130neg-mcp2k work-sent130neg2-mcp1k work-sent130neg2-mcp2k work-sent130neg3-mcp2k work-sent130neg4-mcp2k	the sentinel registry's NEGATIVE controls -- the only on-disk proof the suite CAN fail; regenerable by scripts/pr130_sentinel_neg34.sh but not re-derivable after the fact	docs/pr/127, docs/pr/130, doc 91

# --- added 2026-09-08 (doc 103): THE LATEST CAMPAIGN ----------------------
# Doc 102 (2026-09-08) produced the four-sample PR validation set for
# colleagues at WCT master eacacafe: 3067 events, ref/prod-2026-09-08.
# MEASURED, not assumed: this campaign is entirely SELF-CONTAINED.  A symlink
# census over both target spellings (28415 links) finds ZERO cross-arm edges
# out of the eight d102m/d102mpr dirs -- the 12268 links inside them are all
# intra-arm `ql_evt<N>/... -> ../evt<N>/...`, 0 broken.  So it borrows nothing
# from grp0825, d97fv or any earlier campaign, and no earlier campaign can be
# released "through" it.
work-mcp1k-d102m work-mcp2k-d102m work-ncpi0-d102m work-nuecc48-d102m	**LATEST PRODUCTION stage A** (imaging + clustering + Q/L), ref/prod-2026-09-08, 3067 events, toolkit eacacafe = master.  Supersedes work-*-d97fv	doc 102 sec 0, ref/prod-2026-09-08/README.md
work-mcp1k-d102mpr work-mcp2k-d102mpr work-ncpi0-d102mpr work-nuecc48-d102mpr	**LATEST PRODUCTION stage B** (the 15-stage tagger + PR chain), ref/prod-2026-09-08, 3067 events; the source of products/prod0908/*-scores-prod0908.tsv.  Supersedes work-*-d144fixprod	doc 102, products/prod0908/
ref/ products/	the generation records and the published score tables; not work dirs, named here so no future planner's grammar has to be lucky	doc 102 sec 6

# --- STAGED FOR RETIREMENT 2026-09-08 (doc 103 tier 2) -- NOT YET EXECUTED ---
# The three lines below are still ACTIVE protections.  They are listed here
# because doc 103 stages them for release and the release is the owner's
# CONFIRM, not mine.  When `CONFIRM=yes retire_20260908.sh 2 sbnd` has run,
# move these three protections into the RETIRED section with the date; until
# then they protect exactly as before.
#
#   work-*-grp0825      superseded by work-*-d102m (which re-images itself)
#   work-*-d97fv        superseded by work-*-d102m      -- prod-2026-09-08
#   work-*-d144fixprod  superseded by work-*-d102mpr    -- prod-2026-09-08
#
# Grounds, costs and the measurements behind them: plan_20260908.py TREES,
# tier2_sbnd_20260908.txt, and docs/103_cleanup-latest-campaign-retire.md.
# Record layer already frozen (67522 files hashed, manifests verified 12/12)
# in archive/records/cleanup-20260908/sbnd-tier2/.
# ORDER IS NOT OPTIONAL: materialise_20260908.py must run first, or the five
# PROTECTED arms that still borrow from grp0825 lose their imaging.
