# doc sbnd_xin/pr/147 round 2 A/B index -- the receiver-side bare tail (177536)
# BEFORE https://www.phy.bnl.gov/twister/bee/set/8e8a9c3c-c2bb-44ca-b94f-91f749afe599/event/list/
#        = work-d147-tailondump-mcp2k  (current production: round-1 flip live, tail absorb OFF)
# AFTER  https://www.phy.bnl.gov/twister/bee/set/118d3404-9708-4fd6-ac60-5c2ac58aacb1/event/list/
#        = work-d147-tailon2dump-mcp2k (+ long_muon_cathode_bridge_tail_min_len=20)
# Same binary both sides; the only difference is that one value.
#
# bee_idx	event	status
0	177536	RESCUED: this was your idx 2, "should be a long muon as well".  Round 1's reading of it was WRONG and this round corrects it -- the piece that was missing is NOT a second cathode half held out by the gap cap.  It is a 279.6 cm bare |13| segment (17008) sharing graph vertex 17005 with an 11.6 cm MEMBER (17009) of the shower the bridge had already built, on the SAME side of the seam: the muon crosses the cathode and then keeps going, and the bare-chain walk only ever looked forward from the partner, never back along the receiver.  ON: one muon node of 917.3 MeV instead of two (276.0 + 644.3 = 920.3), one muon rest mass instead of two (add 219.9 -> 114.3), Enu 1339.8 -> 1231.0 -- within 9 MeV of its pre-regression 1222.4.
#
# What to judge: whether 177536 is now ONE muon from the interaction point,
# across the cathode, out to x = -193.
#
# Over-reach, measured on 17 events: this moves 177536 and NOTHING else.  The
# controls are the events where the same geometry holds but the neighbour is a
# hadron -- 168526 (206.3 cm pion tail), 169824 (153.7 cm pion), 99716 (135.1 cm
# proton), 97231, 98470, 59025, 313847, 288287 -- all byte-identical, because
# the tail must ALREADY be |13|-typed and this walk retypes nothing.  Of the
# bare segments >= 20 cm adjacent to a muon shower's member over 1368 events,
# 37 are protons and 18 pions against 9 muons, so that pdg test is the guard.
