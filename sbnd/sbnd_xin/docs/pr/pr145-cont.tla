# doc sbnd_xin/pr/145 item 3b -- log-only instrumentation for the BFS
# particle-continuation detector (flag_reduce).  Emits SPDLOG lines and nothing
# else; no arithmetic reads it, so it cannot change reconstruction.
kine_continuation_debug=true
